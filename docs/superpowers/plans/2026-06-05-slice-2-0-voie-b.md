# Slice 2.0 voie b (context-pack) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Finir le context-pack Meso minimal — un type `ContextPack{target, last_week_actuals, constraints[], signals[]}` + son builder `build_context_pack(snapshot, prev_week)`, avec la réduction forward-only `actuals_from_week`.

**Architecture:** Tout pur domaine. `meso/model.py` (stdlib only) porte les types + la réduction ; `meso/context.py` (bridge) assemble le pack depuis un `WorldSnapshot` déjà chargé. Forward-only : les actuals entrent par chaînage d'une semaine typée précédente, `None` au cold-start. `signals[]` = slot typé laissé vide (rempli en 2.1). Aucune DB, aucun LLM, aucun réseau.

**Tech Stack:** Python 3, dataclasses frozen, pytest. Branche : `codex/runtime-v0-reliability`.

**Spec :** `docs/superpowers/specs/2026-06-05-slice-2-0-voie-b-design.md`

> **Note commits (CLAUDE.md) :** chaque task se termine par un commit, mais CLAUDE.md impose de ne committer que sur demande explicite de Loïc. À l'exécution, confirmer le go avant chaque `git commit` (ou laisser Loïc batcher).

---

## File Structure

- `backend/src/fitmas/runtime_v0/meso/model.py` — **Modify** : ajoute `Signal`, `ContextPack`, `actuals_from_week`. Reste pur (stdlib only).
- `backend/src/fitmas/runtime_v0/meso/context.py` — **Modify** : ajoute `build_context_pack`. Seul module Meso qui voit `WorldSnapshot` (déjà le cas).
- `tests/runtime_v0/test_meso_model.py` — **Modify** : tests réduction + types.
- `tests/runtime_v0/test_meso_context.py` — **Modify** : tests builder.
- `docs/BUILD-ORDER.md`, `docs/superpowers/specs/2026-06-05-moteur-sport-plan.md` — **Modify** : marquer Slice 2.0 fait.

---

### Task 1: Réduction forward-only `actuals_from_week`

**Files:**
- Modify: `backend/src/fitmas/runtime_v0/meso/model.py`
- Test: `tests/runtime_v0/test_meso_model.py`

- [ ] **Step 1: Write the failing tests**

Dans `tests/runtime_v0/test_meso_model.py`, ajouter `import pytest` en tête (après `from __future__ import annotations`) et `actuals_from_week` à l'import depuis `meso.model`. Puis ajouter en fin de fichier :

```python
def test_actuals_from_week_reduces_build_week():
    week = PlannedWeek(
        sessions=(
            _s(1, "easy_run", 45, "easy"),      # 45
            _s(3, "threshold", 60, "hard"),     # 120  <- la clé qualité
            _s(6, "long_run", 90, "moderate"),  # 135
        )
    )
    actuals = actuals_from_week(week)
    assert actuals.total_load == 300.0
    assert actuals.key_type == "threshold"


def test_actuals_from_week_rejects_zero_key():
    week = PlannedWeek(
        sessions=(
            _s(1, "easy_run", 45, "easy"),
            _s(6, "long_run", 90, "moderate"),
        )
    )
    with pytest.raises(ValueError):
        actuals_from_week(week)


def test_actuals_from_week_rejects_multiple_keys():
    week = PlannedWeek(
        sessions=(
            _s(2, "threshold", 60, "hard"),
            _s(5, "intervals", 50, "hard"),
        )
    )
    with pytest.raises(ValueError):
        actuals_from_week(week)
```

L'import devient :

```python
from fitmas.runtime_v0.meso.model import (
    PlannedWeek,
    TypedSession,
    WeekActuals,
    actuals_from_week,
    derive_continuity_target,
)
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `pytest tests/runtime_v0/test_meso_model.py -k actuals_from_week -v`
Expected: FAIL — `ImportError: cannot import name 'actuals_from_week'`

- [ ] **Step 3: Write minimal implementation**

Dans `backend/src/fitmas/runtime_v0/meso/model.py`, ajouter en fin de fichier (après `derive_continuity_target`) :

```python
def actuals_from_week(week: PlannedWeek) -> WeekActuals:
    """Reduce a typed week to last-week actuals (forward-only chaining).

    A generated, verified build week has exactly one quality key; the precondition
    holds by construction. Recovery/taper (legitimately 0 key) is out of scope for
    Slice 2.0 — the generator decides the carried key_type then.
    """
    keys = [session for session in week.sessions if session.is_quality_key]
    if len(keys) != 1:
        raise ValueError(
            f"forward-only actuals need exactly one quality key, got {len(keys)}"
        )
    return WeekActuals(total_load=week.week_load, key_type=keys[0].type)
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `pytest tests/runtime_v0/test_meso_model.py -k actuals_from_week -v`
Expected: PASS (3 tests)

- [ ] **Step 5: Commit**

```bash
git add backend/src/fitmas/runtime_v0/meso/model.py tests/runtime_v0/test_meso_model.py
git commit -m "feat(meso): forward-only actuals_from_week reduction

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: Types `Signal` + `ContextPack`

**Files:**
- Modify: `backend/src/fitmas/runtime_v0/meso/model.py`
- Test: `tests/runtime_v0/test_meso_model.py`

- [ ] **Step 1: Write the failing tests**

Dans `tests/runtime_v0/test_meso_model.py`, ajouter `ContextPack` et `Signal` à l'import depuis `meso.model`, puis ajouter en fin de fichier :

```python
def test_signal_holds_kind_and_text():
    sig = Signal(kind="fatigue", text="jambes lourdes")
    assert sig.kind == "fatigue"
    assert sig.text == "jambes lourdes"


def test_context_pack_defaults_signals_empty():
    pack = ContextPack(
        target=None,
        last_week_actuals=None,
        constraints=(),
    )
    assert pack.signals == ()
    assert pack.target is None
    assert pack.last_week_actuals is None
```

L'import devient :

```python
from fitmas.runtime_v0.meso.model import (
    ContextPack,
    PlannedWeek,
    Signal,
    TypedSession,
    WeekActuals,
    actuals_from_week,
    derive_continuity_target,
)
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `pytest tests/runtime_v0/test_meso_model.py -k "signal or context_pack" -v`
Expected: FAIL — `ImportError: cannot import name 'ContextPack'`

- [ ] **Step 3: Write minimal implementation**

Dans `backend/src/fitmas/runtime_v0/meso/model.py`, ajouter ces deux dataclasses **après** `TypedConstraint` et **avant** `derive_continuity_target` :

```python
@dataclass(frozen=True)
class Signal:
    """A recent fact as an advisory hint for the generator (never a gate)."""

    kind: str  # "fatigue" | "soreness" | "preference" — non sur-enumé en V0
    text: str


@dataclass(frozen=True)
class ContextPack:
    """The layered Meso context-pack the generator/verifier consume.

    target/last_week_actuals are None at cold-start (no prior typed week).
    signals is a typed slot left empty in Slice 2.0 (filled in 2.1).
    """

    target: WeekTarget | None
    last_week_actuals: WeekActuals | None
    constraints: tuple[TypedConstraint, ...]
    signals: tuple[Signal, ...] = ()
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `pytest tests/runtime_v0/test_meso_model.py -k "signal or context_pack" -v`
Expected: PASS (2 tests)

- [ ] **Step 5: Commit**

```bash
git add backend/src/fitmas/runtime_v0/meso/model.py tests/runtime_v0/test_meso_model.py
git commit -m "feat(meso): Signal + ContextPack typed layers

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: Builder `build_context_pack`

**Files:**
- Modify: `backend/src/fitmas/runtime_v0/meso/context.py`
- Test: `tests/runtime_v0/test_meso_context.py`

- [ ] **Step 1: Write the failing tests**

Dans `tests/runtime_v0/test_meso_context.py`, étendre les imports puis ajouter les tests. En tête, ajouter `from datetime import date` (en plus de l'import datetime existant) et adapter les imports projet :

```python
from fitmas.runtime_v0.meso.context import (
    build_context_pack,
    constraints_from_snapshot,
    fact_to_constraint,
)
from fitmas.runtime_v0.meso.model import (
    PlannedWeek,
    TypedConstraint,
    TypedSession,
    WeekActuals,
)
```

Ajouter en fin de fichier :

```python
def test_build_context_pack_cold_start():
    snapshot = _snapshot((_fact("health", 0.9, 1),))
    pack = build_context_pack(snapshot, prev_week=None)
    assert pack.last_week_actuals is None
    assert pack.target is None
    assert len(pack.constraints) == 1
    assert pack.constraints[0].restricts == ("intensity",)
    assert pack.signals == ()


def test_build_context_pack_with_prev_week_derives_target():
    snapshot = _snapshot((_fact("health", 0.9, 1),))
    prev_week = PlannedWeek(
        sessions=(
            TypedSession(date=date(2026, 6, 1), type="easy_run", duration_min=45, intensity="easy"),      # 45
            TypedSession(date=date(2026, 6, 3), type="threshold", duration_min=60, intensity="hard"),     # 120
            TypedSession(date=date(2026, 6, 7), type="long_run", duration_min=90, intensity="moderate"),  # 135
        )
    )
    pack = build_context_pack(snapshot, prev_week=prev_week)
    assert pack.last_week_actuals == WeekActuals(total_load=300.0, key_type="threshold")
    assert pack.target is not None
    assert pack.target.key_type == "threshold"
    assert pack.target.load_band == (300.0, 330.0)  # build 100% -> 110%
    assert len(pack.constraints) == 1
    assert pack.signals == ()


def test_build_context_pack_without_health_fact_has_no_constraint():
    # active_facts is already filtered upstream; a resolved/expired health fact
    # is simply absent -> no constraint in the pack.
    snapshot = _snapshot((_fact("preference", 0.9, 2),))
    pack = build_context_pack(snapshot, prev_week=None)
    assert pack.constraints == ()
    assert pack.signals == ()
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `pytest tests/runtime_v0/test_meso_context.py -k build_context_pack -v`
Expected: FAIL — `ImportError: cannot import name 'build_context_pack'`

- [ ] **Step 3: Write minimal implementation**

Dans `backend/src/fitmas/runtime_v0/meso/context.py`, étendre l'import depuis `meso.model` et ajouter le builder en fin de fichier.

Remplacer la ligne d'import `from fitmas.runtime_v0.meso.model import TypedConstraint` par :

```python
from fitmas.runtime_v0.meso.model import (
    ContextPack,
    Phase,
    PlannedWeek,
    TypedConstraint,
    actuals_from_week,
    derive_continuity_target,
)
```

Ajouter en fin de fichier :

```python
def build_context_pack(
    snapshot: WorldSnapshot,
    prev_week: PlannedWeek | None = None,
    phase: Phase = "build",
) -> ContextPack:
    """Assemble the layered context-pack from a loaded snapshot (pure, no I/O).

    Forward-only: actuals enter by chaining the previous typed week. prev_week is
    None at cold-start -> target/actuals are None (the generator seeds in 2.1).
    signals stays empty in Slice 2.0.
    """
    actuals = actuals_from_week(prev_week) if prev_week is not None else None
    target = derive_continuity_target(actuals, phase) if actuals is not None else None
    return ContextPack(
        target=target,
        last_week_actuals=actuals,
        constraints=constraints_from_snapshot(snapshot),
        signals=(),
    )
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `pytest tests/runtime_v0/test_meso_context.py -k build_context_pack -v`
Expected: PASS (3 tests)

- [ ] **Step 5: Commit**

```bash
git add backend/src/fitmas/runtime_v0/meso/context.py tests/runtime_v0/test_meso_context.py
git commit -m "feat(meso): build_context_pack assembles the layered pack

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 4: Régression complète, LOC, docs

**Files:**
- Modify: `docs/BUILD-ORDER.md`
- Modify: `docs/superpowers/specs/2026-06-05-moteur-sport-plan.md`

- [ ] **Step 1: Run the full runtime_v0 suite (no regression)**

Run: `pytest tests/runtime_v0 -q`
Expected: PASS, aucune régression (le total monte de 8 nouveaux tests vs la baseline 201).

- [ ] **Step 2: Check core LOC vs cap 3700**

Run:
```bash
find backend/src/fitmas/runtime_v0 -name '*.py' -not -path '*__pycache__*' -not -name '* 2.py' -print0 | xargs -0 wc -l | tail -1
```
Expected: total < 3700. Si ≥ 3700 : couper (commentaires/docstrings d'abord, `Signal` minimal). Noter le chiffre.

- [ ] **Step 3: Update BUILD-ORDER.md**

Dans `docs/BUILD-ORDER.md`, section "Chantier Moteur Sport", marquer Slice 2.0 fait : `signals` (slot vide typé) + `last_week_actuals` (voie b forward-only via `actuals_from_week`) + `ContextPack` + `build_context_pack` codés. Pointer vers `docs/superpowers/specs/2026-06-05-slice-2-0-voie-b-design.md`. Suite immédiate : **Slice 2.1** (générateur `propose_week`).

- [ ] **Step 4: Update the day plan**

Dans `docs/superpowers/specs/2026-06-05-moteur-sport-plan.md`, table des slices : passer la ligne **2.0 — Contrat contexte** à fait (✅), avec une note "voie b forward-only, signals slot vide, cold-start None".

- [ ] **Step 5: Commit**

```bash
git add docs/BUILD-ORDER.md docs/superpowers/specs/2026-06-05-moteur-sport-plan.md
git commit -m "docs(meso): mark Slice 2.0 (context-pack, voie b) done

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Self-Review

**Spec coverage :**
- `ContextPack` + `Signal` typés → Task 2. ✅
- `actuals_from_week` forward-only (raise si ≠1 clé) → Task 1. ✅
- `build_context_pack(snapshot, prev_week, phase)` cold-start None → Task 3. ✅
- `signals` slot vide typé → Task 2 (défaut) + asserts Task 3. ✅
- constraints via bridge existant → réutilisé Task 3 (pas de re-code). ✅
- Tests model (réduction, 0/2 clés) + context (cold-start, prev_week, no-health) → Tasks 1/3. ✅
- meso/ pur, aucune I/O → respecté (builder n'utilise qu'un snapshot chargé). ✅
- Docs à jour → Task 4. ✅

**Placeholder scan :** aucun TBD/TODO ; chaque step montre le code réel.

**Type consistency :** `actuals_from_week` / `build_context_pack` / `ContextPack` / `Signal` nommés identiquement partout ; signatures alignées sur `derive_continuity_target(actuals, phase)` et `constraints_from_snapshot(snapshot)` existants ; bandes (`300.0 -> 330.0`) cohérentes avec `_PHASE_BANDS["build"] = (1.00, 1.10)`.
