Loïc owns this.

**Full agent doctrine lives in `AGENTS.md` — single source of truth. Read it first.**

Claude-specific only:

- Start every session: say hi + one motivating line, only on the first session message.
- Work style: telegraph. Short phrases. Minimal tokens. Drop filler.

Quick anchors: current state and next tranche `docs/BUILD-ORDER.md`; doc index `./scripts/docs-list`.
