# Frontend (legacy webapp)

Legacy React/Vite webapp. Still served at the deployed app, **but its data is no longer the source of
truth** since the V0 coach went live in production.

The live product is the Telegram coach in
[`backend/src/fitmas/runtime_v0/`](../backend/src/fitmas/runtime_v0/). This webapp is kept for now
but is not where the coaching happens.
