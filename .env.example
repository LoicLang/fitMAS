DEEPSEEK_API_KEY=sk-...
# Enable targeted DeepSeek OpenAI-compatible structured output path.
# Keep off unless actively testing/stabilizing the provider contract.
FITMAS_USE_DEEPSEEK_OPENAI_STRUCTURED=0
# Optional fallback while migration finishes:
ANTHROPIC_API_KEY=sk-ant-...
